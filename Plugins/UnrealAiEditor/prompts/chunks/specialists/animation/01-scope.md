# Scope

Typical tools: `level_sequence_create_asset`, `sequencer_open`, `animation_blueprint_get_graph_summary` (availability depends on catalog `status`).

Finish with `<unreal_ai_specialist_result>...</unreal_ai_specialist_result>`.
